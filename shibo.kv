#:kivy 1.1.0

LoadCalendar:

<LoadCalendar>:

    BoxLayout:
        size:root.size
        TextInput:
            text:''
        Button:
            text:"送信"
            on_release: root.googleBotton()